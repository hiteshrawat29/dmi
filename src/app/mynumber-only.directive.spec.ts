import { MynumberOnlyDirective } from './mynumber-only.directive';

describe('MynumberOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new MynumberOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
