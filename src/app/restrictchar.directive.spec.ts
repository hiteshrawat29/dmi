import { RestrictcharDirective } from './restrictchar.directive';

describe('RestrictcharDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictcharDirective();
    expect(directive).toBeTruthy();
  });
});
